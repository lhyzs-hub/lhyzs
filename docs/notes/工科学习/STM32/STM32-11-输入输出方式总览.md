---
title: STM32 输入输出方式总览
created: 2026-08-07
updated: 2026-08-15
tags:
  - 领域/嵌入式
  - 主题/STM32
  - 主题/输入输出
  - 类型/速查表
  - 难度/入门
aliases:
  - STM32 IO总结
  - STM32输入输出总结
---

# STM32 输入输出方式总览

> 函数中的句柄、缓冲区、长度、超时和通道不能省略理解。统一参数表见 [STM32-HAL函数速查](STM32-HAL函数速查.md#_2)，各外设详细参数见对应专题笔记。

> [STM32-09-内部结构与总线架构](STM32-09-内部结构与总线架构.md) · [STM32-10-GPIO内部结构与模式](STM32-10-GPIO内部结构与模式.md) · [STM32-HAL函数速查](STM32-HAL函数速查.md)

!!! abstract "选择方法"
    先判断信号是数字量、模拟量、脉冲还是协议数据，再决定外设；然后根据速率和实时性选择轮询、中断或 DMA。

## 一、按信号类型分类

| 外部信息 | STM32 输入方式 | STM32 输出方式 |
|---|---|---|
| 高/低电平 | GPIO Read、EXTI | GPIO Push-Pull/Open-Drain |
| 电压大小 | ADC | DAC（部分型号）或 PWM+滤波 |
| 脉冲数量 | 定时器外部计数、编码器模式 | 定时器输出比较/PWM |
| 脉冲时间 | 输入捕获 | 输出比较、单脉冲模式 |
| 字节流 | UART、SPI、I²C、CAN、USB | 相同通信外设 |
| 时间 | RTC、定时器、SysTick | 闹钟、中断、时间戳 |

## 二、数字输入方式

### 1. GPIO 轮询

```c
GPIO_PinState value = HAL_GPIO_ReadPin(KEY_GPIO_Port, KEY_Pin);
```

- CPU 主动询问当前电平。
- 简单、容易调试；频繁轮询浪费 CPU，短脉冲可能漏掉。
- 适合按键、拨码和变化很慢的状态。

### 2. 外部中断 EXTI

- 在上升沿、下降沿或双边沿时通知 CPU。
- 适合按键事件、模块告警、低频脉冲。
- 只能告诉你“边沿发生了”；电平保持时间和高速计数应使用定时器。

### 3. 定时器外部计数/编码器

- 硬件自动统计脉冲，CPU 不用逐个响应。
- 编码器模式同时读取 A/B 相，得到方向和相对位移。
- 适合转速、流量计、旋转编码器。

### 4. 输入捕获

- 边沿到达时硬件保存计数器值。
- 用差值测周期、频率、脉宽和超声波回波时间。

## 三、数字输出方式

### 1. GPIO 电平输出

```c
HAL_GPIO_WritePin(CTRL_GPIO_Port, CTRL_Pin, GPIO_PIN_SET);
```

用于使能、片选、方向和 LED。推挽主动高低，开漏需上拉。

### 2. 软件翻转

主循环或中断中定时改 GPIO。只适合低速、时序要求不严格的信号；CPU 忙或被中断会产生抖动。

### 3. 定时器输出比较

计数器到达 CCR 时改变输出或产生事件。适合精确边沿、周期事件和单脉冲。

### 4. PWM

硬件周期输出方波，频率由 PSC/ARR 决定，占空比由 CCR 决定。适合亮度、功率、舵机、电机和音调控制。

### 5. PWM + DMA

DMA 连续更新 CCR，可产生 WS2812、波形表、步进脉冲序列等复杂时序。

## 四、模拟输入输出

### ADC 输入

```text
模拟电压 → 采样保持 → ADC量化 → 数字结果 → CPU/DMA
```

- 单次轮询：偶尔测一次。
- 定时器触发 + DMA：固定采样率和连续数据流。
- 多通道扫描：按 Rank 顺序写入数组。

### 模拟输出

- 有 DAC 的型号：数字值直接转换为电压。
- F103C8 无通用 DAC：用 PWM+低通滤波获得平均电压，或外接 I²C/SPI DAC。
- PWM 平均值不是对所有负载都等价于真正模拟电压。

## 五、通信接口选择

| 接口 | 线路 | 时钟 | 双工 | 典型用途 | 关键特点 |
|---|---|---|---|---|---|
| UART | TX、RX | 异步 | 全双工 | 调试、蓝牙、GPS | 简单，无共享时钟 |
| I²C | SCL、SDA | 同步 | 半双工式共享 | 传感器、OLED | 多设备地址、开漏上拉 |
| SPI | SCK、MOSI、MISO、CS | 同步 | 全双工 | 屏幕、Flash、ADC | 快，无统一设备协议 |
| CAN | CANH、CANL，经收发器 | 异步仲裁 | 总线 | 工业、汽车 | 差分、仲裁、可靠 |
| USB | D+、D- | USB时钟 | 双向 | 设备与电脑 | 协议复杂，需要USB栈 |

!!! warning "接口名称不等于电气层"
    STM32 UART 引脚是 3.3 V TTL/CMOS 电平，不能直接接 RS-232；CAN 必须经过 CAN 收发器；USB 也不是普通 UART。

## 六、按 CPU 参与程度分类

| 方式 | CPU 做什么 | 优点 | 缺点 | 适合 |
|---|---|---|---|---|
| 轮询 | 不断检查状态/等待完成 | 最简单 | 阻塞、浪费 CPU | 首次验证、低速偶发操作 |
| 中断 | 事件发生时执行 ISR | 响应快、CPU 可做别的 | 高频时开销大、并发复杂 | 按键、少量字节、定时事件 |
| DMA | 硬件在外设与内存间搬运 | 高吞吐、低 CPU 占用 | 配置复杂、缓存生命周期重要 | ADC采样、UART/SPI 数据块 |
| 外设自治 | 定时器触发 ADC/DMA 等链路 | 时序最稳定 | 调试门槛高 | 实时采样、波形输出 |

```mermaid
flowchart LR
    POLL[轮询] --> IT[中断]
    IT --> DMA[DMA]
    DMA --> AUTO[定时器触发+DMA+外设自治]
```

不是越右越好。先用最简单方式验证硬件，再按吞吐量、实时性和 CPU 占用升级。

## 七、阻塞与非阻塞

### 阻塞式

函数返回前一直等待操作完成或超时，例如 `HAL_UART_Receive(huart, pData, Size, Timeout)`：依次传入串口句柄、接收缓冲区、接收字节数和超时毫秒数。

### 非阻塞式

启动后立即返回，完成时进入回调或改变状态，例如 `HAL_UART_Receive_IT(huart, pData, Size)` 和 `HAL_ADC_Start_DMA(hadc, pData, Length)`。异步完成前缓冲区必须持续有效。

非阻塞调用的缓冲区在完成前必须保持有效；不能使用即将退出函数的局部数组。

## 八、输入输出数据流模板

分析任何外设时填写：

| 问题 | 示例：UART DMA接收 |
|---|---|
| 物理输入是什么？ | RX 引脚电平 |
| 谁进行采样/解码？ | USART2 |
| 数据放在哪里？ | SRAM 的 `rx_dma[]` |
| 谁搬运？ | DMA1 对应通道 |
| 如何知道完成？ | IDLE/半传输/全传输事件 |
| CPU 最终做什么？ | 解析帧、产生业务命令 |
| 超时或错误怎么办？ | 清错误、重启接收、统计故障 |

## 九、场景选择速查

| 需求 | 推荐方式 |
|---|---|
| 读取普通按键 | GPIO 轮询 + 消抖；需要唤醒时 EXTI |
| 统计高速脉冲 | 定时器外部计数，不用 GPIO 中断逐个数 |
| 测脉宽/频率 | 定时器输入捕获 |
| 串口调试打印 | UART 轮询发送即可起步 |
| 串口不定长命令 | DMA + IDLE + 循环缓冲区 |
| 连续多通道采样 | 定时器触发 ADC + Circular DMA |
| 呼吸灯/电机调速 | 定时器 PWM |
| WS2812 | 定时器 PWM + DMA |
| 多个低速传感器 | I²C |
| 高速屏幕/Flash | SPI + DMA |

## 十、复习问题

1. 为什么短脉冲不适合主循环轮询？
2. 中断和 DMA 的“完成”分别意味着什么？
3. 为什么开漏输出写 1 之后还需要上拉？
4. ADC 连续采样为什么适合定时器触发而不是 `HAL_Delay()`？
5. UART 黏包为什么不是硬件故障？

资料：[STM32F103C8 外设概览](https://www.st.com/en/microcontrollers-microprocessors/stm32f103c8) · [RM0008 参考手册](https://www.st.com/resource/en/reference_manual/cd00171190-stm32f101-103-105-107-stm32f100-series-armbased-32bit-mcus-stmicroelectronics.pdf)

<!-- lhyzs-note-nav:start -->
---
> ← 上一篇：[STM32 GPIO 内部结构与模式](STM32-10-GPIO内部结构与模式.md) · 下一篇：[STM32 HAL 函数速查](STM32-HAL函数速查.md) →
<!-- lhyzs-note-nav:end -->
